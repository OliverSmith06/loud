const classes = {
  terminalSquiggle: {
    color: "#0c8ce9",
    fontSize: "1.2em",
    marginRight: "0.5%",
  },
};

function TerminalSquiggle() {
  return <div style={classes.terminalSquiggle}>{"~"}</div>;
}

export default TerminalSquiggle;
