const classes = {
  terminalArrow: {
    color: "#b837eb",
    fontSize: "1.2em",
  },
};

function TerminalArrow() {
  return <div style={classes.terminalArrow}>{">"}</div>;
}

export default TerminalArrow;
